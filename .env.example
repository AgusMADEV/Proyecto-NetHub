# ==================================================
# Archivo .env.example
# Copia este archivo a .env y completa tus datos reales.
# NUNCA subas el archivo .env a un repositorio git.
# ==================================================

# ── SMTP (Envío de correo) ─────────────────────────
SMTP_SERVER=smtp.ionos.es
SMTP_PORT=587
SMTP_USER=tucuenta@tudominio.com
SMTP_PASSWORD=tu_contraseña_smtp

# ── IMAP (Lectura de correo) ───────────────────────
IMAP_SERVER=imap.ionos.es
IMAP_PORT=993

# ── IA Remota jocarsa ──────────────────────────────
IA_REMOTA_URL=https://xxxx-xxxx.ngrok-free.app/api.php
IA_REMOTA_KEY=TEST_API_KEY_JOCARSA_123

# ── API REST y Autenticación ───────────────────────
JWT_SECRET_KEY=cambia-esta-clave-por-una-muy-segura-en-produccion
# Genera una clave segura con: python -c "import secrets; print(secrets.token_urlsafe(32))"

